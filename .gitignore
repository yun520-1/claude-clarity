node_modules/
data/
memory/
auth.json
.env
*.onnx
self-verification.json
*.log
.DS_Store
src/core/.upgrade-manifest.json
UPGRADE_REPORT.txt

# 本地开发遗留模块（不进入 clawhub.ai 发布）
src/legacy/

# 本地历史升级报告（与 clawhub.ai 发布无关）
upgrades/
downloads/

# 安全：HMAC 密钥 — 不应提交到仓库
memory/.hmac-key

# 安全：AES 加密密钥文件 — 不应提交到仓库
memory/.aes-key
.aes-key
internal/

# 安全：npm 认证令牌 — 不应提交到仓库
.npmrc

# 测试覆盖率报告（由 npx jest --coverage 生成）
coverage/